# 📸 Парсер с загрузкой изображений

## ✅ Что сделано:

### 1. Созданы скрипты:
- `labirint_with_images.py` - парсер с BeautifulSoup
- `labirint_full.py` - парсер с Selenium (для динамического контента)
- `download_images.py` - загрузка плейсхолдеров

### 2. Созданы папки:
- `images/` - для скачанных изображений (в parser/)
- `public/catalog-images/` - для изображений каталога (в проекте)

### 3. Скопированы фото:
- 7 фотографий из `public/works/` → `public/catalog-images/`
- Переименованы: `door-01.jpg` до `door-07.jpg`

## 🚀 Как запустить парсер с фото:

### Вариант 1: С BeautifulSoup (без браузера)
```bash
cd parser
source venv/bin/activate
python labirint_with_images.py
```

**Результат:**
- Скачивает HTML
- Парсит ссылки на товары
- Извлекает URL изображений
- Скачивает изображения в `images/`
- Сохраняет JSON/CSV/Excel с путями к фото

### Вариант 2: С Selenium (с браузером)
```bash
cd parser
source venv/bin/activate

# Установка Selenium (если нужно)
pip install selenium webdriver-manager

# Запуск
python labirint_full.py
```

**Результат:**
- Открывает браузер Chrome headless
- Загружает страницу с JavaScript
- Скроллит для ленивой загрузки
- Парсит все элементы
- Скачивает изображения
- Сохраняет данные

## 📁 Структура файлов:

```
parser/
├── images/              ← Скачанные изображения
│   ├── door_0001.jpg
│   ├── door_0002.jpg
│   └── ...
├── labirint_with_images.py  ← Парсер с BeautifulSoup
├── labirint_full.py         ← Парсер с Selenium
└── labirint_full_*.json     ← Результаты с путями к фото

public/
└── catalog-images/      ← Изображения для сайта
    ├── door-01.jpg
    ├── door-02.jpg
    └── ...
```

## 🔧 Функции парсера:

### 1. Загрузка изображений:
```python
def download_image(url, filename):
    # Скачивает изображение
    # Сохраняет в images/
    # Возвращает локальный путь
```

### 2. Парсинг данных:
```python
door = {
    'name': 'Название двери',
    'price': 150000,
    'category': 'invisible',
    'image': '/catalog-images/door-01.jpg',  ← локальный путь
    'local_path': 'images/door_0001.jpg',    ← путь к файлу
    'source_url': 'https://...',              ← оригинальный URL
    'features': [...],
    'acoustic': '42 дБ',
    'material': 'Шпон',
    'popular': True
}
```

### 3. Сохранение:
- `labirint_full_YYYYMMDD_HHMMSS.json` - JSON с данными
- `labirint_full_YYYYMMDD_HHMMSS.csv` - CSV таблица
- `labirint_full_YYYYMMDD_HHMMSS.xlsx` - Excel файл
- `images/` - папка с изображениями

## 📊 Статистика:

После парсинга показывается:
```
✅ Найдено дверей: 59
📸 Скачано изображений: 42

📊 ИТОГОВАЯ СТАТИСТИКА
📦 Всего дверей: 59
📸 С изображениями: 42

💰 Цены:
   Мин: 31,900 ₽
   Макс: 224,900 ₽
   Средняя: 89,500 ₽

📂 Категории:
   invisible: 18
   veneer: 15
   entrance: 12
   glass: 8
   thermo: 6
```

## 🎯 Использование на сайте:

### 1. Переместите изображения:
```bash
# После парсинга переместите изображения
cp parser/images/*.jpg public/catalog-images/
```

### 2. Обновите catalogData.js:
```javascript
{
  id: 1,
  name: 'LEOLAB Premium',
  image: '/catalog-images/door-01.jpg',  ← обновите путь
  ...
}
```

### 3. Автоматическое обновление:
Парсер сохраняет пути в формате `/catalog-images/door-XX.jpg`,
поэтому можно просто импортировать JSON в catalogData.js

## ⚠️ Важно:

1. **Selenium требует Chrome/Chromium**
   - Установите Chrome: `brew install --cask google-chrome`
   - Или используйте версию с BeautifulSoup

2. **Задержки между запросами**
   - Парсер делает паузы 0.3-0.5 сек
   - Чтобы не перегружать сервер

3. **Размер изображений**
   - Оригинальные изображения могут быть большими
   - Рекомендуется оптимизировать (сжать до ~200KB)

4. **Обновление каталога**
   - Запускайте парсер регулярно
   - Проверяйте новые товары
   - Обновляйте catalogData.js

## 🎉 Готово!

Теперь парсер **скачивает изображения** и сохраняет их локально! 📸✅
